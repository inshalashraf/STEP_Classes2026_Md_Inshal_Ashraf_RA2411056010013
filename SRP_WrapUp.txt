BankAccount is now focused on basic account operations instead of handling everything itself. AccountRepository handles database saving, NotificationService handles notifications, and StatementGenerator handles statement formatting. Splitting these responsibilities makes each class smaller and easier to understand and test. A change in one responsibility should not require changes to the other classes.